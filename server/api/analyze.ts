// Placeholder — analysis endpoint.
// Will receive { jobOffer, cv } and return structured AI analysis.

export default defineEventHandler(async (_event) => {
  return { message: 'Not implemented yet.' }
})
